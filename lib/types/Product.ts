
// Define a interface do produto (usando os mesmos campos da sua entidade)
export interface IProduct {
    idproduct: number;
    name: string;
    price: number;
    quantity: number;
    desc: string;
    img: string;
    createdAt: string;
    updatedAt: string
}